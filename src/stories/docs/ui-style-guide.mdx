import { Meta, Markdown } from '@storybook/addon-docs/blocks';
import { linkToRepo } from '../support/repo-links';
import { GuideLink } from '../support/guide-link';
import guide from '../../../docs/ui-style-guide.md?raw';

<Meta title="Guides/UI style guide" />

> Rendered from `docs/ui-style-guide.md` when the gallery is built. Edit that file, not this page.
> Its links into the repository open on GitHub.

<Markdown options={{ overrides: { code: 'code', a: GuideLink } }}>{linkToRepo(guide, 'docs/ui-style-guide.md')}</Markdown>
