# Joint type as one choice, sliders as one joint, cylinders as a sealed slide

> **Status:** Built — plan of record from September 15, 2026. It replaces the
> bodies-and-joints migration, which lives on the unmerged branch `bodies-and-joints-plan` in
> `docs/bodies-and-joints-plan.md` and was never on `staging`; Stage R retires it.
> **Stages R, 0, 1 and 2 are built:** a joint's type is one choice of four in the Edit panel, in
> the group panel and at the top of the right-click menu, a slider is a single `PrisJoint` carrying
> its own mass and `rotates`, and a cylinder is a record looked up from its seal, with a selectable
> slide and a panel each for its barrel and its rod. Split Joint (D7) is the one deferral and has
> yet to be scoped. Everything here is built on `staging` in ordinary pull requests, on the public
> editor, with the existing solver.

## Why

The public editor stores a drawing as points with flags and infers the rest from geometry. A
slider was three objects at one point (a `PrisJoint`, a zero-length `SliderBlock`, a coincident
`RevJoint`) until Stage 1, and a cylinder is five joints and three links that `model/cylinder.ts`
re-recognizes by collinearity and `model/cylinder-pose-plan.ts` repairs after every edit. That inference is
where the slider and cylinder bug classes come from, and the two-toggle joint panel (Slider,
Welded) hides the four things a joint can actually be.

This plan does three things in order, each shippable on its own: (0) present the joint's kind
as one four-way choice on today's model; (1) make a slider one joint; (2) make a cylinder a
sealed slide with two named members. The simulation layer is not redesigned: the solver keeps
receiving the points it receives today, produced by a small lowering step where the stored
form no longer contains them.

## The design of record

The UI is settled and drawn at real size in the Claude Design project
`edb9e4a9-b7bd-4257-8f2d-6c82d9fa8a90`, file **`Joint Type Spec.dc.html`** (`Joint Type
Redesign.dc.html` is the exploration behind it; `github.md` there lists the `staging` files each
artboard was built from). Read it with the DesignSync tool (`get_file`) after `/design-login`, or
export it; the glyphs are inline SVG in the spec. It contains: the control and its measurements,
the four glyphs in three inks plus a grounded set, eleven panel states, six menu states.

The decisions it encodes, with the maintainer's answers of September 15 folded in:

| # | Decision |
| --- | --- |
| D1 | **Joint Type** is one `segmented-block` with four values, wrapped into two columns (one added rule, `flex-wrap: wrap` with a 50% basis, and a story). It is the first control under Joint Position in Basic Settings. The values are today's two bits: Revolute = neither; **Pin-in-slot** = Slider; **Prismatic** = Slider + Welded (the phase-3 "Slide": rider rigid with the block); **Welded** = Welded. |
| D2 | **Grounded stays** as the toggle it is, under Joint Type. When Grounded is on, all four glyphs swap to the grounded set. |
| D3 | **Slider Angle** is its own row, under Grounded, present only while Grounded is on and the type is Prismatic or Pin-in-slot. A floating slot has no angle to type: its direction is the carrier's two joints. |
| D4 | The **dangling slider** state is kept as the one inline state: a 15px Material glyph, bold lead "Nowhere to slide.", sentence "Drag it onto a link to cut its slot, or ground it.", the chosen cell in refusal ink. The carrier is still assigned by dropping the joint on a link; there is no carrier picker. |
| D5 | Every other refusal rides the control's hover tooltip; nothing else pops into the layout. A value the joint cannot take is drawn in `--text-disabled`. |
| D6 | **Mass Settings** appears on a joint whose type has a block (Prismatic, Pin-in-slot). In Stage 0 it reads and writes the `SliderBlock`'s mass; from Stage 1 the joint's own. |
| D7 | **Add Input** stays. The spec pairs it with a new **Split Joint** action in a `dual-button` row. Split Joint (the inverse of merging by drop) is **deferred** to its own pull request after Stage 2; until it ships, Add Input renders full-width as today and the menu footer is unchanged. |
| D8 | **Menu:** the four values are a grid at the top of the card (82px) above the ladder; State keeps Grounded, Driven Input, Locked in that order; Traces and the destructive footer are unchanged; a ticked row keeps its check in the right-hand slot and a refusal takes that slot instead. Subtitles stay the public ones (`Pin · Links AB, BG`, `Slider · Link BC`, `Ground pin · Barrel AS`). |
| D9 | **A cylinder's slide** is a joint: the square drawn mid-skin. Selecting it shows Joint Type with Prismatic chosen at full ink and the other three refused "inside a cylinder"; **Add Input is live** (the cylinder's drive is this joint's); no Grounded row ("ground an end joint instead" in the menu; the spec drew it as "ground a mount instead", and *mount* is a code word that `docs/ui-vocabulary.md` keeps out of the UI); no Travel field (the stroke is the barrel's length); a **Starts at** field in percent of stroke. |
| D10 | **A cylinder has one angle.** Barrel Angle, Rod Angle and the slide's Slider Angle are the same value; editing any of them rotates the assembly about the slide joint by default. Standard constraint solver logic applies where if one of the mounts are grounded, it rotates about said joint. |
| D11 | **Starts at** (extension at the design pose): by default the rod mount moves along the axis to satisfy it; if the rod mount is grounded, the barrel mount moves; if both mounts are grounded, a member length changes (the rod's unless it holds its length, then the barrel's); if both lengths are held, or the request cannot be met, the edit is refused with the notification wording for too many constraints and nothing changes. *(September 20, 2026: the first two rungs stand and the rest does not — **S17** puts the typed number above a grounded joint, and then the maintainer reversed the third rung too: with both mounts grounded the part **expands and contracts**, and a member's length changes only when neither end joint may move at all. Only two joints that are genuinely locked reach a refusal, and under **S19** even that first goes as far toward the number as it can.)* |
| D12 | **Barrel and rod** are named members (`Edit Barrel AS`, `Edit Rod SB`) with Length and Angle as hold fields; Draw as a Disc is greyed; there is no Add tracer point and no Add force on a member (absent, not greyed); Mass Settings as on a link. |
| D13 | **A mount is a pin like any other**: its type is live, it attaches like one, and its delete row names the cascade ("Delete Joint (and Cylinder)"). |
| D14 | **Every count the reader sees** (Lock All, Delete entire mechanism, "and N links") counts visible joints and links, never the solver's hidden points. |
| D15 | Freeze banners, the lock banner, Rename/Lock/Delete, Visual Settings, Distance to Joints are unchanged. |
| D16 | New words, to be added to `docs/ui-vocabulary.md` in Stage 0: Joint Type, Revolute, Prismatic, Pin-in-slot, Welded (as a type), Slider Angle, Starts at, Barrel, Rod, "inside a cylinder", "Nowhere to slide.", Split Joint (deferred). |

## Rules for whoever runs this

- **Branch from `origin/staging`; every package is a pull request against `staging`.** Never push
  `main` or `staging`. One package per PR, each green on CI and on the suites its section names.
- The public editor's current behavior is the contract except where a decision above changes it.
  Everything a reader sees must match the spec artboards; when the spec and the app's existing
  block disagree, the block wins and the spec is corrected, never a new stylesheet.
- Every new block state gets a story and the gallery sweep passes (`node .storybook/tools/sweep.mjs`).
  New colors are tokens. Words come from `docs/ui-vocabulary.md`. `npm run check` before every push.
- Verify in the running app with Playwright and look at the screenshots; a filmstrip for anything
  that moves. Add what you found to a tracked `e2e/*.mjs` suite. Do not run the whole e2e batch;
  run the suites the package names.
- Update `CLAUDE.md`, `docs/short-notes.md` and `docs/README.md` in the PR that changes what they
  describe. Regenerate `docs/fixture-urls.md` and template payloads when a fixture changes.
- These packages are sized for Opus agents working in parallel on disjoint files; the orchestrating
  session reviews diffs, runs the full unit suite and pushes. Fable is not required.

## Stage R — retire the bodies-and-joints migration (one docs PR)

`staging` no longer carries the provider seam (reverted in PR #23), so nothing on staging depends
on the migration. Retire it in one PR:

- `docs/bodies-and-joints-plan.md`, `docs/bodies-and-joints-progress.md`,
  `docs/chrome-provider-seam.md`, `docs/native-ui-parity-plan.md` (where present on staging):
  status line → *Retired, September 15, 2026*, one paragraph pointing here and saying why
  (the migration is superseded by this plan; the branch and its verified solver stay as reference).
- `docs/README.md`: move those entries under a "Retired" note; add this plan under Design records.
- `CLAUDE.md`: remove any sentence about the native route, provider tokens or S5–S8.
- On GitHub: close PR #13 with a comment naming this plan; keep `bodies-and-joints-plan`,
  `backup/pr13-before-seam-20260914` and `backup/bodies-and-joints-before-staging-20260913`.
- Memory/agent notes: nothing else.

## Stage 0 — the choice, on today's model

No model or codec change. The four-way control writes the existing `slider` and `weld` form
controls that `EditPanelComponent` already binds (`jointForm`), and `MechanismService.toggleSlider`
/ the weld toggle keep doing the work. Ships as three PRs.

**0a. Glyphs and the block (files: `src/assets/icons/`, `src/app/app.component.ts` icon
registration, `src/app/component/BLOCKS/segmented/`, `src/stories/blocks/`).**
Lift the nine line-art glyphs (Revolute, Prismatic, Pin-in-slot, Welded, each floating and
grounded, plus Split for later) from the spec's inline SVG into `assets/icons/joint-*.svg`,
stripped of metadata, 24px box, 2px strokes, `currentColor`; register them beside the other
SVG icons. Give `segmented-block` a wrapping mode (two columns, 50% basis, 32px rows, 2px gap,
glyph 20px + 8px gap, label 12.5px, chosen cell `--surface` with the spec's shadow, disabled
cells in `--text-disabled` with a tooltip) and one story per state: four options wrapped,
chosen/not chosen/refused, grounded set. Gate: gallery sweep; `npm run check`.

**0b. The panel (files: `component/edit-panel/edit-panel.component.{html,ts,scss}`,
`docs/ui-vocabulary.md`).** Replace the Slider and Welded toggles with Joint Type: value derived
from the two bits, writes go through the existing toggle paths so a change of type is one undo
entry (two bits flipping at once must still be one entry: batch them in one structural edit).
Slider Angle becomes its own row (D3). The dangling warning becomes the inline state (D4). Mass
Settings shows for a block (D6, reading the `SliderBlock`). Refusals from `gridUtils.sliderRefusal`
and the weld refusal move onto the control's tooltip and the refused cell. Add Input unchanged
(D7). Gate: `e2e/disabled-toggles.mjs`, `cylinder-panel`, `phase4-build-from-scratch`, `mobile`,
`ui-copy`; a new `e2e/joint-type.mjs` that walks a pin through all four values on a grounded and a
floating joint and asserts the bits, the undo count and the panel rows per state.

**0c. The menu (files: `services/context-menu-builder.service.ts`,
`component/BLOCKS/context-menu/*`, `src/stories/shared/context-menu.stories.ts`).** Add a
"choice grid" element to the menu model and renderer (2×2, keyboard-reachable inside the CDK
menu, one story), place it at the top of a joint's card, remove the Slider and Welded rows from
State, keep the rest (D8). Gate: `e2e/context-menu.mjs`, `context-menu-modes`, `keyboard-shortcuts`.

## Stage 1 — one joint per slider

**What changes in the model.** A slider becomes a single `PrisJoint` in `joints[]`: links attach
to it directly; it carries `mass` (the block's) and `rotates` (true for Pin-in-slot, false for
Prismatic, which is today's Welded bit on the coincident pin). `SliderBlock` and the coincident
`RevJoint` are removed. The carrier, slot joints, slot angle, dangling and well-formed rules on
`PrisJoint` stay as they are.

**1a. Model and codec (files: `model/joint.ts`, `model/link.ts`, `services/transcoding/*`,
`model/mechanism/mechanism-partition.ts`).** A prismatic joint entry gains mass; the block link
entry and the pin entry are no longer written. The reader accepts the old form (PRISMATIC bit +
block link + coincident pin) and folds it, and the checked-in production fixtures under
`src/test-data/verification` and `src/test-utils/verification/fixture-gallery.ts` must decode to
identical geometry. Undo replays URLs, so the fold must be exact.

> **Built with no version bump, by structural recognition instead** (decided in the pull request
> for 1a). The old form is named by what its records *are* — a piston link record joining a
> prismatic joint to a coincident pin — rather than by a discriminator, so a new payload needs no
> new version and every old one folds. The cost is one-directional: an **older** build handed a
> new payload stops reading a joint record after `driveSpeed` with no arity check, the digest
> still matches, and it drops the mass token — opening a grounded slider with a bar hanging off
> it and no block. That is a wrong drawing rather than a refusal, and it is why `docs/fixture-urls.md`
> now says every slider row needs a Stage 1 build. `src/test-data/legacy-payloads.ts` freezes the
> seventeen payloads the last release before Stage 1 actually emitted, and a spec decodes each
> against its regenerated twin.

**1b. Solver and forces (files: `model/mechanism/loop-solver.ts`, `position-solver.ts`,
`kinematic-solver.ts`, `force-solver.ts`, `rigid-bodies.ts`, `bodies.ts`).** Loops now pass
through the prismatic joint rather than a block link; the point-on-line constraint and the
Slide's orientation coupling are the same equations. The block's mass becomes a point body at
the joint in the force solver. Gate: `app.component.spec.ts` (MATLAB sixbar), the verification
tables, `fixture-gallery` and `template-payloads` specs, `e2e/template-graphs.mjs`,
`playback-timing`, `force-units`, `analysis-editing`: every number identical to before.

> **Met, with one recorded exception.** `e2e/template-graphs.mjs` is 3978/3978, the MATLAB sixbar
> is byte-identical, and the unit suite runs with nothing skipped. Two rate defects this stage
> introduced were caught in review and fixed in it rather than deferred: the drive row being
> dropped from the system the *rates* are solved through (the scissor lift, and every shape whose
> drive mounts the walk places), and `determineLoops` never recording a ground-to-ground chain one
> edge long (the elliptical trammel, which is that shape only now that a slider is one joint).
>
> The exception is **`Cylinder_Gripper`'s D and Q**, re-pinned in `template-baseline.ts`. That
> machine's simultaneous system is square but rank-deficient — a 2-D nullspace — so least squares
> wanders inside it, and a slider being one unknown rather than two coincident ones makes the
> system a column narrower and the wander land differently. Both old and new values are below the
> 1/1000-unit the URL can carry and both satisfy the suite's own `toBeCloseTo(_, 3)`; they are now
> pinned at the mirror symmetry the drawing actually has rather than at whichever number the
> search settles on, which is what makes them self-enforcing. Nobody owes a further fix: the
> wander is a property of that mechanism, not of this stage.

**1c. Canvas, panel, menu, deletion (files: `component/new-grid/*`, `services/slider-mark.service.ts`,
`model/joint-marks.ts`, `services/grid-utils.service.ts`, `services/mechanism.service.ts`
weld/slider/delete paths, `edit-panel`, `context-menu-builder`).** One mark per slider (the block
glyph), one hitbox, one letter; the two-marks-at-one-point ambiguity is gone by construction.
Delete cascades and counts count the one joint (D14). Mass Settings reads the joint. Gate:
`e2e/phase4-gestures.mjs`, `phase1-drag`, `creation-previews`, `locking`, `link-holds-angles`,
`edit-undo`, `posed-editing`, `joint-type`.

**1d. Fixtures, templates, docs.** Regenerate `docs/fixture-urls.md` and template payloads;
`e2e/template-open.mjs` 11/11; `CLAUDE.md` model section (Links: no `SliderBlock`; a slider is a
`PrisJoint` with mass and `rotates`); `docs/short-notes.md`.

## Stage 2 — the cylinder as a sealed slide

**What a cylinder is after Stage 1.** Barrel link A–N (N its inner end), rod link S–B, and S a
prismatic joint (`rotates: false`) riding the barrel's slot, sealed. The seal is the existing
`isSealed` bit on S. A **cylinder record** is derived, not stored: `{ seal: S, barrel, rod,
mountA: A, mountB: B, start }` where `start` is S's position along the barrel at the design pose
as a fraction of the stroke (the barrel's length less the head clearance, `cylinderStroke`).
N is owned by the seal: hidden, unlettered, no hitbox, its position derived as A plus the barrel
length along the axis; it exists for the solver only.

**Decisions taken when Stage 2 was scoped** (September 19, 2026). The section above says what a
cylinder is; these are the questions it left open, answered once so the four packages agree.

| # | Decision |
| --- | --- |
| S1 | **The slot's order is the role.** `seal.slotJointA` is mount A and `seal.slotJointB` is N; the rod is the one two-joint bar riding the seal and its other end is mount B. Nothing is measured to find a role. Creation already writes the slot in that order. An old payload does not promise it, so the **reader** is the one place the old distance rule survives: on decode each sealed slot is put in order (the barrel joint further from the rod's mount is A), once, before anything asks. |
| S2 | **N and S are derived, the mounts are not.** After every rebuild the axis is A→B, N goes to A plus the barrel's length along it and S to B minus the rod's length. For a drawing that is already straight this writes nothing. It never clamps S into the travel (the reason `normalizedCylinderPose` gave still holds) and never moves a mount. It is two coordinate writes, not an edit: it does not go through `planEdit`, which stays as the transaction every *edit* of a cylinder runs in. |
| S3 | **Barrel and rod have their own lengths.** "Equal by construction" goes, because D11 and D12 change one without the other. The travel is still the barrel's alone (`cylinderStrokeAlong`), the span is S's place along the barrel plus the rod, and a new cylinder is still drawn with the two equal, so every number an existing drawing produces is the number it produced before. The rod has a floor, the stroke, so mount B never retracts past the barrel's mouth. |
| S4 | **A mount dragged past a stop** resizes both members by the same amount, which is today's rule when they are equal. A member holding its length does not resize and the other takes all of it; with both held the mount stops at the stop. |
| S5 | **One angle, two padlocks.** A member holds one thing, like any bar, and the URL's `H` entry stays one per link. Either member holding `angle` holds the cylinder's angle, solved mount to mount as it is today; releasing it from either panel clears both. Fixing the angle writes it on a member holding nothing, the barrel first, so a rod can hold its length while the cylinder holds its angle; with both lengths already held it replaces the length of the member whose padlock was pressed, the way a bar's does. A held `length` is not handed to the hold solver: the layout in S4, D11 and the length edits is what honors it. |
| S6 | **Lengths.** Barrel Length moves N and nothing else, so the stroke and Starts at change with it; it is refused below the length that still holds the head where it stands, and above the one that would take the stroke past the rod. Rod Length moves B; if B is grounded A moves (with N and S); if both are grounded S slides, and the edit is refused when that leaves the travel. A refused length changes nothing and says why through `NotificationService`. *(September 20, 2026: the three refusals here are not refusals any more — **S17** repairs each of them with the smallest change to the other, unfixed member, and puts the typed number above a grounded joint; **S19** takes whatever is still out of reach as far toward the typed number as it goes and says what stopped it. The last sentence stands: what is refused changes nothing and says why.)* |
| S7 | **Dragging S** is Starts at by hand: S follows the pointer along the axis between the stops, and D11's first two rungs decide which end gives. A drag never changes a length, so with both end joints grounded S stays where it is and the number is typed into Starts at instead. Typing its X or Y goes through the same door. *(September 20, 2026: the second sentence is gone — **S17** makes a drag user input exactly as a typed number is, so with both ends grounded the part expands and contracts under the pointer instead of standing still. A drag and the field answer a both-grounded ram the same way. A drag's refusals are still silent, and it is the one edit **S19** leaves alone: it already clamps at a stop without saying so.)* |
| S8 | **A Lock** on the barrel, the rod or S is one mark on S and holds all four joints, which is what a lock on the cylinder has always meant and what every shared URL with one says. A Lock on a mount holds the mount. |
| S9 | **Letters.** A new cylinder's mounts take the next two letters, as today, and S the third; N keeps an interior name (`A1`). An old payload's S has an interior name (`A2`), which now reads on the canvas, in a panel title and in an export column. The reader gives such an S the next free letter as the last step of the build and recomputes the ids of the links holding it; nothing else in a payload names S by then. N is never renamed. *(September 21, 2026: the three visible letters are handed out **along the part** instead — the end the gesture started from, then the slide, then the far end, so a cylinder drawn on an empty grid reads A, B, C down its own axis rather than A, C, B. Started from an existing joint, that joint keeps its letter and the slide and the far end take the next two; ended on an existing joint, the slide still takes the letter after the first end's. Duplication letters a copied cylinder the same way, whichever of its joints the selection reached first. **Nothing already drawn is renumbered**: the reader's pass over an old payload still gives an interior-named S the next free letter and touches nothing else, so every URL in circulation decodes to the letters it was written with.)* |
| S10 | **Names.** Barrel = A's name + S's; Rod = S's + B's; Cylinder = A's + B's, as today. |
| S11 | **`isCylinderInterior` was two questions.** *Hidden* is N alone: no hitbox, no letter, no count. *Inside a cylinder* is N and S: placed by the layout, never an anchor for a hold, never welded, merged onto or cut a slot in. |
| S12 | **Dragging a member drags the cylinder**, as dragging the skin always has. Only the selection, the panel and the menu are the member's own. |
| S13 | **The slide's mark.** A slider whose riders cannot turn — the Joint Type Prismatic, floating or grounded, and every cylinder's seal S — is drawn as a cream rounded bar lying along its slot (2.8R by 1.4R, corner 0.25R, `MARK.slide*` in `model/joint-marks.ts`), in place of the `+` it used to wear. A Pin-in-slot slider keeps its circle and a welded *revolute* keeps its `+`, so the mark's shape says whether the riders may turn and its orientation says what they slide along. The mark shrinks with a piston head too short to hold it, never past `MARK.slideHostShare` of that head. **The mark is the joint and the black block under it is furniture**: the block and the head carry the gesture — a click on either still grabs the slider — and every state is drawn on the bar, which takes the joint's fill and color family, the hover class, the selection ring *inside* its own edge (it has one; a `+` does not), the lock badge with no chip, the tutorial ring and `id="joint_<id>"`. Nothing paints the block: the seal's outline path is gone, and S is drawn by the ordinary joint layer above the skin rather than by the skin, leaving N the only joint a cylinder hides (S11). |
| S14 | **A member's inertia is derived.** A barrel and a rod take a mass and nothing else: their moment of inertia and their center of mass follow their own shapes, and no surface offers either as a number. The Barrel and Rod panels show Mass plus the sentence the retired Edit Cylinder panel carried; the linkage table and the analysis setup's table refuse the same edit; the center-of-mass mark on a member is a glyph rather than a drag handle. One predicate answers for all four (`memberInertiaIsDerived` in `model/cylinder.ts`). This is what keeps the decode's migration honest: every cylinder URL in circulation carries the custom flags frozen on both members, the codec has no marker that could tell those from a value somebody typed, and a panel that let one be typed would be promising to keep a number the next decode throws away. A welded compound that merely *contains* a member is an ordinary body and keeps its own controls. |
| S15 | **The rod's own color.** A rod is drawn in its barrel's color until somebody gives it one of its own — one boolean, `RealLink.ownColor`, and `rodFillOf` in `model/cylinder-skin.ts` is the one place the question is asked. It exists because every rod already *stores* a color the skin has never drawn (creation hands each new link the next off the palette), so reading that record would have recolored every drawing in circulation at once; a new cylinder is now created with the two fills equal and the flag off, which is what "it starts as the same color" means on file as well as on screen. Both members carry a color field in the Edit panel, and `paintCylinderMember` is the one rule both doors quote: painting the rod gives it its own color; painting the *barrel* of a rod that has chosen nothing first hands the rod the color it was standing in, so neither ever drags the other along. Each is one undo entry. It rides the URL as `KR<link>` in the `K` part-color family — valueless, because the color is the link's own field, which the format has always carried — and only for rods that were asked, so **every existing URL re-encodes byte-identically and no drawing changes color**. An entry naming a link the URL does not carry is refused like its siblings; one naming a link that is not a rod is accepted and ignored, because unsealing a cylinder leaves the app writing exactly that. |
| S16 | **A welded end is one body.** Weld a bar to a mount and the bracket and the member draw as one rigid body, exactly as two ordinary welded links do: one fill, one continuous outline, a fillet in the elbow. The body's Boolean union takes the member's **skin silhouette** — the barrel's real profile, or the rod's from behind the head to its end joint, built by the skin's own path builders in `model/joint-marks.ts` and placed by `memberSilhouette` in `model/cylinder-fusion.ts` — where it used to leave the leaf out altogether. `RealLink.skinSilhouette` carries it, told to the leaf beside `drawnByACylinderSkin` by the one pass that resolves the structures, because nothing reachable from a compound knows the shape. The union is asked for the member's cut planes *eased* (`CYLINDER.cutEase`), so the fillet softens the elbow and leaves the barrel's mouth and the rod's back square. Color follows the body: `barrelFillOf` and `rodFillOf` answer with the root's fill for a welded member, `paintCylinderMember` writes a member's recolor onto the body holding it, and a rod that has chosen nothing still follows its barrel — which is the bracket once the barrel mount is welded. **The layers do not move**: a body holding a barrel is painted in the barrel's place, under the head block, and one holding a rod in the rod's place over it, so `fusedBodiesOf` assigns each body one pass (the rod's wins where one body holds both) and the links layer leaves it alone. The member's own region still selects the member (S12) through a transparent hit path over the fused fill. **And the body is named by what a reader can see**: a link id is the sorted ids of its joints, so a body welded to a barrel mount held N and was tagged, titled, subtitled and listed as `AA1D` — a joint the drawing never shows (D14, S11). `visibleBodyName` in `model/body-label.ts` drops it, and every reader-facing name asks that one function through `MechanismService.visibleBodyName` / `bodyLabel`; the id itself is untouched, because the URL, the solver, the export columns and the DXF layers are keyed on it. *(September 21, 2026: the last clause is half wrong and **S20** replaces it. The URL and the solver are keyed on the id and stay that way; the export columns and the DXF layers are read by a person, and no longer carry it.)* Mass, inertia and center of mass are untouched — they are summed from the leaves, never from the painted outline — and the DXF is untouched too: `outlineLoops()` gives the export the body *without* the member, because a cylinder is already exported as its own barrel and rod on their own layer and a fused face would lay a second barrel over the first. *(September 21, 2026: "one rigid body" is the drawn and simulated truth and was never the editing one. **S21** says so: only a drag of the body carries what is welded to a member, and every other edit lets this body change shape around the joint it moved.)* |
| S17 | **One ladder for what gives.** Every typed or dragged edit of a cylinder resolves through one priority order, stated once in `whatGives` (`model/cylinder-edit.ts`) and quoted by all five of them: **1. a locked item never moves** — a Lock mark on a joint, a bar elsewhere whose hold reaches it, a member fixed at its length; **2. the number the reader just typed or dragged is honored**, and is refused only when those locks leave no way to honor it; **3. a grounded joint yields**, because ground is only where the frame happens to be pinned; **4. a floating joint is the cheapest thing in the drawing**, so it goes first. As rungs: a floating end joint (the rod's before the barrel's), then the seal where the edit left it free, then a grounded end joint, then the *other* member's length, then nothing. **The maintainer reversed the last two on September 20, 2026**, after driving the first build of this: a length is a number the reader chose and typed, and ground is not, so a cylinder with both ends grounded **expands and contracts** to satisfy a typed extension or a drag of the head rather than quietly resizing a member — and a member changes length only when no end joint is allowed to move at all. That reverses D11's "if both mounts are grounded, a member length changes". **A structural limit is repaired by the smallest change to the other, unfixed member, not refused**, and the repairs are not rungs — no joint move can fix either of them, so they happen before the ladder is climbed: a rod typed under the barrel's travel shortens the barrel to `rod + clearance`, a barrel whose travel outgrows the rod lengthens the rod to exactly that travel, and a barrel that no longer reaches where the head stands puts the head on its nearer stop and lets the ladder decide who follows it. The bottom of the ladder is reachable **only with both end joints held**, because moving an end joint is the one rung that never fails. A refusal names what is holding it, in the sentences `heldBySentence` and the lock wording already use, and changes nothing. Typing into a field that is itself fixed still makes the typed number the new fixed number; it is always the *other* constraint that refuses. |
| S18 | **A slider’s plate is the union with what is drawn.** A Slide fuses its riders to its black block and paints the two as one outline; it built that outline from the rider’s plain link path, which is the thin bar its two joints describe — and for a cylinder member that is not what is on the canvas. So the barrel came out as two nested outlines and the rod as two shapes side by side. One function answers what a rider contributes, `drawnOutlineOf` in `model/cylinder-fusion.ts`: the member’s **skin silhouette** through `memberSilhouette` — the same builder S16 hands a bracket, so a plate and a weld cannot disagree about the shape of one part — and the body’s own path for everything else, which for a body that has already swallowed a member is that union. It is measured at the pose it is asked about rather than read off `skinSilhouette`, which is frozen at the design pose: a compound’s union is built once and rigidly re-placed, a plate is rebuilt every frame. The ink is `fillShownOn`, so a rod that has chosen no color of its own is plated in its barrel’s (S15) rather than in a palette color nothing draws. **The layering is S16’s rule, one joint further out**: a plate holding a member is painted in that member’s place in the skin’s stack — a barrel’s under the head block, a rod’s over it — because the slider layer is under every skin and a rod plate drawn there loses the darker band inside the bore. `fusedBodiesOf` therefore routes plates as well as bodies, one pass per shape, the plate winning over the body inside it; the slider layer then draws the black block alone. **Pin-in-slot fuses nothing**: no plate, and no rider either, because the skin already draws that member above the block — hoisting it here too was the plain bar showing through the barrel. Two smaller things fell out. A **seal** plates nothing and claims nothing: it is a piston head inside its own barrel and the skin draws the whole part, and its claim on the rod’s body is what left a welded rod’s Slide with a bare black block. And a **ram’s bore is not an ordinary channel** in a plate either, which `channels` already knew and `channelsInLocalFrame` did not — invisible while the plate drew a thin bar, and a barrel hollowed into a fork the moment it drew the real one. Selection is unchanged: the member’s region selects the member, the plate carries the rider it stands in for, and the joint is its own cream bar. |
| S19 | **Closest to what was asked.** A typed number the constraints cannot fully honor is honored *as far as it goes*, and the reader is told what stopped it — not refused. The mechanism is one, in `asCloseAs` (`model/cylinder-edit.ts`): the value the part already has is always reachable, because asking for it is the identity, and the values S17's ladder can honor form an interval containing it, so the segment from here to the number typed is searched for the furthest point that works. Closed-form boundaries are tried first so the answer lands *on* one — `rod + clearance` for a barrel against a fixed rod, `cylinderStroke(barrel)` for a rod against a fixed barrel, `cylinderBarrelFloor` for a barrel typed too short, the share that puts the rod on its own floor for *Starts at* — and a bisection over the ladder's own yes/no finds the rest. The answer is an ordinary success carrying `stoppedBy: { code, short, cause, reached }`; `GridUtilsService` commits it as **one** `runEdit`, returns true, and says one sentence through `NotificationService.news` — news, not a refusal, because a refusal is the app's word for nothing having changed. The sentence is "*Barrel AC stopped at 3.29 cm: held by fixed length Rod CB, and a longer barrel would have more travel than the rod is long.*": the model words the cause, the service words the subject (`MechanismService.bodyLabel`) and the number (`NumberUnitParserService`, the same pair the field is formatted with), and the panel's own re-read puts the reached number back in the box. **A refusal survives only where none of the request can be had** — in practice both end joints locked, or a part with no axis to read. A drag is exempt: it already clamps at a stop and says nothing about it, which is the same promise made sixty times a second rather than once. |
| S20 | **The hidden joint is the simulation's alone.** A cylinder's inner end N is never named anywhere a reader reads — not on the canvas, in a panel, a menu, a notification, an `aria-label` or an SVG `<title>`, and **not in a file the app writes**: no CSV column, no JSON field, no DXF layer name, no report drawing. The name to avoid is not only the interior id (`C1`) but every link id built on one, because an id is the sorted ids of its joints: `CC1` is the barrel and `CC1F` is a bracket welded to its mount. S16 left the ids standing in the export columns and the DXF layers on the reading that they are keys; the maintainer overruled that on September 21, 2026 — *"a user should not see it under any circumstance"* — and a key that must stay unique is now disambiguated in words a reader can act on (`AD welded`, then an ordinal) rather than by falling back to the id. `services/export/export-names.ts` is the one answer on the file side, as `visibleBodyName` is on the screen side, and it is deliberately conservative: a body that holds no buried joint keeps its id **to the character**, so every drawing without a cylinder exports byte for byte what it always did. The **one exception is the developer drawer** (right-panel tab 4), which production never reaches and whose whole job is to show the model as the model is. The guard is `e2e/hidden-joint-audit.mjs`, which harvests `innerText`, `aria-label`, `title`, `alt` and SVG text across all four modes, every selection, every right-click card, the right drawer and the exported CSV, JSON and DXF, and matches against the interior names read off the drawing itself rather than against a list written here. |
| S21 | **Only a body drag carries.** A **body motion** — the reader taking hold of a barrel, a rod, or a body welded to either and moving the whole assembly, which is `GridUtilsService.dragCylinder` and `rotateCylinder` and nothing else — carries every bar welded to either member rigidly, and a cylinder inside a body it carries rides along when its far end is free. **Every other edit writes the cylinder's own joints and nothing else**: a dragged end joint, a dragged slide, a typed Angle, *Starts at*, Barrel Length, Rod Length, and a cylinder re-laid because something else moved one of its end joints. A body welded to a member then **changes shape**, exactly as a compound link does when one of its joints is dragged — its other joints stay where the reader put them. The rule lives on the posed entry (`motion: 'body'` in `model/cylinder-pose-plan.ts`), so the planner stays pure and every other caller gets the re-pose by saying nothing. The maintainer reported it twice on **September 21, 2026**: *"In image 1, dragging joint B shouldn't move the location of joint J. Similar to how a standard compound link moves when dragged"*, and *"In image 2, dragging joint A causes B to stay in place but E to move. It should be consistent and it should not move E. E should behave exactly like B."* The file argued the opposite — that moving a member and not the bracket welded to it *tears* the body — and **nothing else in the app believes that**: drag one joint of an ordinary compound and that joint moves while the compound changes shape, and `EditPanelComponent.resolveNewLink` moves exactly one joint for a typed Length or Angle. A welded body is rigid in the *simulation*; in the editor it is a shape somebody is still drawing. Three things follow. A **Lock** on a bracket now stops a body drag and nothing else, so the canvas's drag gate and `frozen` agree where they used to disagree. **Holds are untouched** — `dragJoint` asks `settleHolds` first, and a held bar still gets the CAD answer. And the **both-ends-fused refusal is gone** (`cylinder.both-ends-fused`, and two of `shapeRefusal`'s three `cylinder.pose-conflict` sentences with it): a cylinder whose two end joints are welded into one body now takes a length, and the body changes shape to let it, the way dragging a corner of a welded triangle does. That drawing still never **simulates**, which is `mechanism/readiness.ts`'s to say and not the editor's to forbid. *(September 21, 2026: and readiness's answer turned out to be that it does. **S25** reads a cylinder with one body at both ends as a fixed part of that body rather than as a sliding pair, so the maintainer's fused body on a driven pin runs like any other rigid link. What such a drawing cannot do is stroke.)* What survives is the backstop on the promise `motion: 'body'` makes — a body drag whose pose is not a rigid motion is refused, because the carry would otherwise hand one body two different motions. Both drawings are openable — [image 1](https://app.pmksplus.com/?2v.Ay,1E8.5,0.1011.8A,A,0,0,0.0N,N,tQ,0,0.fC,C,cM,0,0,AJN,A,N.0B,B,1Tm,0,0.0J,J,0ku,0VG,0..YRAJN,AJN,Fe,Fe,2s,0AR,0d125a,A,J,N,,AN,AJ.YRCB,CB,Fe,Fe,123,0,B2DFDB,C,B,,.NRAN,AN,Fe,Fe,Rj,0,c5cae9,A,N,,.NRAJ,AJ,Fe,Fe,0NS,0Fe,303e9f,A,J,,...N_q*3DBVgC) and [image 2](https://app.pmksplus.com/?2v.Ay,1E8.5,0.1011.8A,A,0,0,0.0N,N,tQ,0,0.fC,C,cM,0,0,AMN,A,N.0B,B,1Tm,0,0.0M,M,az,fb,0.fD,D,PZ,Sl,0,AMN,A,M.0E,E,_W,16K,0..YRAMN,AMN,Fe,Fe,Uo,Dt,0d125a,A,M,N,,AN,AM.YRCB,CB,Fe,Fe,123,0,B2DFDB,C,B,,.YRDE,DE,Fe,Fe,i1,nY,26A69A,D,E,,.NRAN,AN,Fe,Fe,Rj,0,c5cae9,A,N,,.NRAM,AM,Fe,Fe,IV,Ko,303e9f,A,M,,...N_r*3f9bEr) — from `src/test-utils/verification/welded-cylinder-fixtures.ts`, which holds the fixtures and the two payloads; the guards are `cylinder-welded-neighbors.spec.ts` and `e2e/cylinder-welded-drag.mjs`. |
| S22 | **An end joint takes a slot drop like any pin.** Drag a cylinder's end joint over a bar and a channel previews in it; release and the slot is cut — the rod end pushing a collar along a rail, which is the textbook arrangement and the one thing a cylinder could not be drawn doing. Everything under the canvas already allowed it: `cutSlotOn` says in as many words that a mount is not covered by its refusals, and `resolveSlotDropTarget` already knew that a member is never a carrier and that a body holding the *other* end would fold the part. The canvas found the candidate and threw it away on the next line, under a comment saying a mount never rides a slot, so the drop was reachable only by calling the service. It is the ordinary gesture now, in the ordinary order — a ring on a joint, or a channel in a bar, or the slot the joint is already riding, and only then the axis snap — and the end joint is re-posed through `dragJoint` like any other, so a joint two cylinders share is still agreed between them. What the cut leaves is what a pin's cut leaves: an unwelded end becomes **Pin-in-slot** and a welded one **Prismatic**, a grounded one gives up its ground because a floating slot's direction is its carrier's, and the joint keeps its letter, its drive and its cylinder's roles. Off again the same way: dragged clear across the bar it detaches and hangs as the red dangling block, and dropped on a bar again it rides. **The one thing that is not an ordinary block is the reseat.** A block that is also an end joint is one end of a rigid part, so `reseatFloatingSliders` plans those moves through `GridUtilsService.runEdit` instead of writing them: the part is re-laid between its two ends and resizes to reach. Written straight, the joint went to the channel with the members the lengths they were, and the derivation that follows drew the head as far outside its own barrel as the stretch. A carrier dragged until the part is closed as far as it goes does not tear and is not refused either: `model/slot-reseat.ts` takes the channel's answer **as far along the channel as the part can follow**, which is what a rail and a collar do, and a Lock on the end joint is left alone outright because a reseat is nobody's gesture. The solver needed nothing: a fixed rail with the drive on the slide, and a rail pinned at one end with the end joint Prismatic, both read one degree of freedom and run. *(September 21, 2026, from the maintainer's "why can't you drag the ends of a cylinder and put it into a slot".)* *(September 21, 2026, later: "the solver needed nothing" was true of the drawing in the session it was drawn in and not of the drawing **reopened**. A URL stores a coordinate on a grain of about a thousandth of a user unit, which left the riding end joint of the maintainer's own scene 7.6e-2 model units off its slot line -- under half a grain -- and the coupled solver refused the whole mechanism for it. **S27** is the fix and the class: it is worse than a reload, because undo replays a URL.)* |
| S23 | **One bar thickness, and it is the rod's.** A bar, a slider's block and a cylinder's rod meet end to end all over a drawing — a rod runs out of its black head and into a pin on an ordinary link — and they were drawn at two widths: the bar at `objectScale / 4`, which is 5/3 R, and the block and the rod at 1.525 R. Nine percent is exactly big enough to see where two of them meet and too small to look deliberate, which is what the maintainer was looking at when they said *"it kind of bugs me that it's very slightly different"*. The rod did not move; **the bar came down to it**. The number has one home now — `BAR_HALF_R` beside `MARK` in `model/joint-marks.ts`, with `BAR_HALF_SCALE` and `barHalfWidth(objectScale)` defined *from* it, because a mark is measured in R and a link outline is built in objectScale and a number said twice is a number that can drift. `MARK.barHalf`, `MARK.blockAcrossHalf`, `MARK.plateFillet` and `CYLINDER.rodHalf` are all that one fact, and a spec pins the four together. Every restatement of `objectScale / 4` asks it instead: the link outline and its compound fillet, the collapsed and circular discs, the creation preview, the Synthesis canvas's pose bars and solution previews, and the DXF's `linkBodyWidth`. What followed it: the weld plate is exactly as wide as its rider rather than a ninth wider, the slot drop radius and the two-bar-widths release distance came in with it, and the **black block is now flush with the bar it rides** instead of sitting inside it with a sliver showing — which reads as a block held by its guide rather than floating in it. The **channel wall** is the one number that got thinner without being asked: a 1.15 R slot in a 1.525 R bar leaves 0.375 R of wall each side where it used to leave 0.517 R, and the drawing was looked at before it was left alone. **Nothing solved changed** — mass, centre of mass and inertia come from `uniformBodyOf(link.joints)`, the joint skeleton and never the painted outline. **The DXF did change**, and it is the one thing here that changes a file the app writes, for every drawing rather than only for cylinders: a body is drawn at the canvas's width, so `linkBodyWidth()` is 0.4575 objectScale where it was 0.5, and the default pin hole — half the body, to two figures — follows it (0.25 cm to 0.23 cm at the gallery's object scale, 0.18 cm to 0.16 cm at the app's default). No expectation was hard-coded to the old number: the specs and `e2e/dxf-sweep.mjs` derive theirs. *(September 21, 2026, from the maintainer's screenshot of a rod pinned to a link.)* |
| S24 | **One paint order for the whole drawing, not a stack per part.** A cylinder's skin is barrel, black head, rod, and the rod drawn over the head at 0.7 alpha is what makes the head read as the darker band that says how much rod is still in the bore. That held only while no two cylinders shared a body. Weld two rod ends to one bar and that body is painted once, in the first cylinder's stack — so the second cylinder's head is painted *after* it and comes out bare `#000`, and the second cylinder's own barrel lies over its own rod. The maintainer reported it as *"when you weld two cylinders together it breaks that height rule"*. So the order is a property of the drawing: `model/cylinder-paint-order.ts` takes the marks and the Slide plates and returns the steps in the order the skin layer paints them — one step per cylinder (its head, and whichever members nothing bigger has swallowed, where the stack holds by construction) and one per fused body or plate. The constraint is one line per cylinder, *whatever paints its barrel* < *its head* < *whatever paints its rod*, which leaves one edge per **fused** member, and a topological sort over them gives every cylinder the same picture however they are welded: two rods in one body puts it after both heads, two barrels in one bracket before both, a chain gives head, shared body, head. **The constraints can contradict each other** — one body holding both members of a cylinder, or body X holding rod 1 with barrel 2 while body Y holds the other two — and the cycle is broken at a **barrel** edge and never at a rod one, which is the judgment `fusedBodiesOf` used to make on its own: a barrel painted over its head is a cue dimmed, a rod painted under one is a cue deleted. A cycle alternates cylinders and bodies, so there is always a barrel edge to break. Every tie is settled by key rather than by input order — the ready set smallest first, the cycle broken at its smallest barrel edge — because `@for` tracking and every hit test read DOM order. The template walks that one list: a cylinder's own group keeps `.cylinder-mark`, a fused body is a sibling step in `.cylinder-fused` (so it needs no frame to undo, and `SliderMarkService.unframe` is gone), and everything that has to sit above every skin — the seal's hit area, the driven arrows, the per-member outlines — moved into one `.cylinder-overlay` pass after all the paint, because a body welded to another cylinder can be painted after this one and a hit area under it is a handle nobody can grab. Neither of the two places a cylinder is drawn a second time has a stack to order: the start-pose ghost is one flat set of silhouettes at one alpha and the report drawing (`mechanism-svg.ts`) is a skeleton of polylines, neither of which paints a head. `e2e/cylinder-mount-render.mjs` draws all four shapes and reads both the DOM order and the pixel in the middle of every head. *(September 21, 2026.)* |
| S25 | **A cylinder inside one body is a fixed part of it, and the machine runs.** A sliding joint is a joint between *two* bodies. Weld a cylinder's two end joints into the same body — or pin the two bodies to each other at two points, which says the same thing — and the seal has one body on either side of it: no relative freedom left to allow, so the part is not a sliding pair at all but a fixed shape inside that body, carried by the body's own motion like any other point of it. `model/cylinder-frozen.ts` is that one predicate (`isFrozenCylinder`), asked of `mechanism/bodies.ts` where a caller has a drawing and of the two roots alone where it has only a joint. **Mobility already agreed, and now says so**: `determineDegreesOfFreedom` charges a joint one less than the number of bodies it holds together, and a slide that holds one body to itself holds one — so it costs nothing, and the maintainer's fused body on a grounded pin counts the one freedom it has. What actually stopped it was the **position solver**: a partition whose every joint rides the input's own body reaches `orderCoupledPartition` with nothing left to solve, and that branch returned `'nothing-to-solve'` without recording the drive's own steps — so `stepCount` stayed at the reset's zero, `attemptPositionAnalysis` ran none of them and reported success, and the frame after it read a position no joint had. The `TypeError` escaped `updateMechanism` before `this.mechanisms` was assigned, the service kept the machine it had solved *before the input was switched on*, and the panel reported that machine's blocker. The **force** model leaves the slide out: what the barrel and the rod press on each other with is internal to a rigid body and statically indeterminate, so there is no guide couple, and the seal exchanges an ordinary pin pair with the body instead of a normal force — two unknowns against the two rows a point body has, which carries the seal's own mass into the body's reactions and reproduces, to solver tolerance, what the same body reports drawn as plain welded bars. A drive on such a seal is refused in the model's words ("*Both of this cylinder's end joints are welded into Link ABCDE, so it cannot extend. Unweld joint A or joint C, or drive a different joint.*"), the menu grays Add Input with `cannot extend`, and readiness warns *A cylinder cannot extend* where it used to say the linkage was binding on a ram using 0% of its stroke. **Round 4's triangle is not this**: two bodies sharing one pin are not one body, so its seal stays a real sliding pair — and the two are rigid *to each other* all the same, because a pin and a Slide between the same two bodies remove all three freedoms between them, so that span cannot change either. The mobility that follows is exactly the rescue `mechanism/mobility.ts` exists for: the pin and the Slide have already made the slot row redundant, Gruebler charges for it anyway and counts **0**, the geometry says **1**, and **1** is the true answer — the rigid pair turns as one body about whatever grounds it. Every other answer that drawing gives is true too: its welded corner refuses a drive because it is welded, and its ram, driven, reports *Nothing moves when the input turns* rather than telescoping. *(September 21, 2026, from the maintainer's "Image 2 shows this incorrect status about nothing being driven because there's something clearly being driven. ... But this one should be simulateable".)* **Amended September 21, 2026:** frozen and **held** (S28) are one state with two causes -- welded shut by the drawing, or holding its length because nothing drives it -- and they share one mechanism, the body merge in `assignBodies`. The force models stay apart, and rightly: what a frozen ram's members press on each other with is internal to a rigid body and indeterminate, while a held ram's is a determinate axial force worth showing. |
| S26 | **Readiness never says something false, and always says something.** Two rules, from the same report. **"Nothing drives this mechanism" may be said only when no joint of that machine has Driven Input on in the drawing the reader is looking at.** The sentence is asked twice now: `Mechanism` clears `input` on every copy outside `ownJointIds`, so its `'not-driven'` and `partition.ownJoints` are two readings of one fact and a disagreement between them is a bug — `ownJoints` rather than everything the partition was handed, because a machine solved against a shared frame piece is handed its neighbor's driven pin and reading *that* would make the sentence unsayable for a machine which genuinely has no drive. Where they disagree the reader gets the other fact instead, naming the joint that is driven. **And a fallback that is honest.** `readinessOf`'s switch had no `default` and `ready` is `isMechanismValid() && no blockers`, so a build that came back invalid carrying a failure with no sentence — or no failure at all — showed a red chip with nothing under it. The switch is exhaustive now (`const unhandled: never = failure`), so a new `MechanismFailure` without a sentence fails the build, and behind it stands one blocker that says what *is* known and what to try: "*This mechanism could not be solved. It has 1 degree of freedom and is driven at joint E, and no motion came out of the pose it starts in. Drag a joint to start it somewhere else, or undo the last change and make it a step at a time.*" No internal word, no error code. The other places that restate "is it driven" were checked against it: `MechanismService.invalidReason` already reads the drawing, the mode chips and the playback rows count these blockers, and `describeActuatorRefusal` is the one answer the menu and the panel both quote. `readiness-honest-blockers.spec.ts` sweeps every `MechanismFailure` against a drawing that has a driven joint and forces the fallback. *(September 21, 2026, from the maintainer's "you should never show a message if a mechanism is driven that says it's not driven. There should be a fallback error when you can't find a good message for it instead of defaulting".)* **And a solve that throws is a machine that could not be solved** (review, the same day): the report was, underneath, an exception that left `updateMechanism` before the machines it was building were stored, so every panel described the drawing from before the edit. That throw is fixed where it was; for the next one, `Mechanism`'s constructor catches it, logs it, and comes back invalid as `'solver-error'`, which the fallback speaks for — a true sentence over a stale one. |
| S27 | **A drawing that ran when it was saved runs when it is reopened.** The maintainer's cylinder-on-a-slot from S22 -- a ternary grounded at `A` with a slot `A–B`, a cylinder whose barrel end `D` rides it, its slide driven, its rod welded into a second grounded ternary -- came back from its own URL reporting *Nothing moves when the input turns*. Nothing was wrong with it. The transcoder packs a coordinate onto a step of about a thousandth of a user unit (measured: nudge a joint by less and the identical number comes back, and the step is the same 0.1 to 0.2 model units on a four-unit punch press and on a two-hundred-unit walking pair, so it is absolute rather than a share of the drawing), which left `D` **7.6e-2 model units off its own slot line** -- under half a grain. `admitCoupledSystem` then asks the drawn pose to satisfy the constraints it implies to `scale * 1e-6`, which here is **5.1e-4**: four hundred times tighter than the format it was judging. It refused, `routeCoupled` named every non-ground joint unsolvable, and the mechanism came back with one sample. **The gate is right and its floor was not.** Refusing a structure somebody has bent by hand is the job, and one part in a million of the mechanism's size is the right *shape* for it -- but a pose does not arrive at full precision, so the tolerance is now `max(scale * 1e-6, 4 * URL_COORDINATE_GRAIN)` through one `drawnPoseTolerance`, shared with `prescribedGeometryHolds` because both ask the same question of the same kind of pose. Four grains is 0.004 of a user unit: two for the worst a row can carry -- a slot-offset row carries the rounding of the riding joint, of the slot's anchor and of the slot's direction -- and two of margin, and twenty-odd times smaller than the radius a joint is drawn at. Nothing moves at decode; `settleInitialPose` puts the admitted pose exactly on its constraints afterwards, which is the job it has always had, and the ordinary walk never reaches either gate so no drawing that already ran is touched. **It is a class and it is worse than a reload**: undo and redo replay URLs, so every undo on such a drawing landed on the refusal -- no cycle, no anchor, no ghost -- and the next drag reseated it and it solved again from wherever it stood, which is very likely what the maintainer was seeing in "the ghost is not synced with the actual start". With the drawing running, `e2e/ghost-is-the-start.mjs` stops skipping it and its invariant holds. `url-round-trip-solves.spec.ts` proves the class -- this drawing and S22's two driven scenes, each with its end joint placed off grid, encoded, decoded and replayed -- and the drawing is published as *Cylinder riding a slot*. *(September 21, 2026.)* |
| S28 | **A cylinder nothing drives holds its length unless the machine itself moves it.** Gruebler is right about the maintainer's triangle of three rams -- five bodies, six full joints, **3** -- and nobody means it. A cylinder nothing drives is, to the person who drew it, a rigid link of the length it was drawn at: a ram with its valves shut, a strut. It cannot be *every* undriven cylinder, because a passive ram is also a perfectly good **follower** -- a telescoping strut between a crank-driven rocker and ground has its length *forced* to change, counts 1 today, runs today, and must go on running exactly as it does. **Held is decided from the geometry, on the same Jacobian mobility is counted from** (`mechanism/mobility.ts`, opened up as `freedomFrameOf`): a row is added for the driven coordinate -- a slide's travel, or the angle `resolveActuator` says the drive turns -- the remaining freedoms are taken, and a passive ram whose slide still moves in one of them is a length nothing is deciding. That is order-independent by construction (every ram is judged against the same subspace) and the tie-break runs over a list sorted by the name a reader reads. **The gate is the whole of why it is safe:** it fires only where the count is above one, which is a drawing that refuses to run today, so nothing that animates now changes -- proved by `npm test`, `e2e/gallery-sweep.mjs` and `e2e/template-graphs.mjs`. Held and **frozen** (S25) are one state with two causes, and share one mechanism: `assignBodies` merges the two members into one body, so the count, the position solve and the forces all see the machine that results. `model/mechanism/cylinder-hold.ts` is the rule; `mechanism/freedoms.ts` is the count, lifted out of `Mechanism` so the mobility a reader is shown and the mobility the rule is judged against cannot be two different numbers. **Nothing is stored** -- it is derived on every build, and the transcoder is untouched. **With no input** there is no coordinate to hold against, so every passive ram that can move at all is a candidate and rams are released, in the reader's order, only as far as leaving one freedom takes; the maintainer's triangle holds the same three either way, and the blocker turns from *3 degrees of freedom* into *Nothing drives this mechanism*, which is the thing actually in the way (S26 cuts both ways). **The reader is told, in a note.** `CheckState` gains `'note'` -- an outline `i` in the muted ink, counted by neither chip -- because nothing here is wrong and a reader who drew three rams and got a rigid triangle would otherwise think the solver is broken: *"Nothing drives any of cylinders AC, CE and EA and the rest of the mechanism does not move them, so each holds the length it was drawn at -- 6.00, 5.83 and 5.83 cm -- and the machine runs with them as rigid links. Right-click the joint inside one of them -- B, D and F -- and switch on Driven Input to make that one extend instead."* `Degrees of freedom` reads the machine's mobility, **1**, and the note is what explains the difference. Never "locked" (a Lock is about position) and never "frozen", which is a word for the code. **Forces are determinate here, unlike S25's frozen case:** barrel and rod are two bodies joined by the slide -- a normal force and a couple -- plus the hold, which is three unknowns, exactly the driven ram's set with the drive's effort as the axial one. So the hold is an unknown column like the drive's, and it is shown: **Holding Force**, on the slide's own panel, beside where a driven ram shows Input Force. Both oracles hold: swap a held ram for a plain bar of the same mass and every external reaction and the input torque agree to twelve digits, and the seal's own free body closes with the hold in it. **Two things in the solver had to move.** The coupled route interpolates its boundary along *chords* while halving a step, and a body turning through an arc does not pass through its chords' midpoints -- it arrives very slightly **shrunk**, which no pose can satisfy once two unknowns are pinned rigidly to two different boundary joints. `halfwayBoundary` takes the square root of the fitted rigid motion instead. And a solved position is stored rounded to four decimals, so a boundary placed joint by joint is not quite rigid either; a held system is therefore accepted at `heldPoseTolerance` -- two terms, the drawing's own size for the lever and a score of grains for the walk -- asked for by name, never assumed, because loosening it for everything costs the guard that refuses a six-bar converging onto the wrong assembly mode (`boundary-driven-branch.spec.ts`). Published as *Three cylinders in a triangle*, *Four-bar on a held cylinder*, *Telescoping strut* and *One follower and one surplus*; `src/tests/verification/cylinder-held.spec.ts` and `e2e/cylinder-held.mjs` are the guards. *(September 21, 2026, from the maintainer's "This one should also simulate no?")* |
| S29 | **Object Size is how a drawing is drawn. It never moves a joint a reader can see, and it never leaves a cylinder in two pieces.** Every other part of the app already held that: a bar's outline is rebuilt at the new width, a joint is drawn at the new radius, and nothing about the linkage changes. A cylinder was the exception, because the *drawn* head and the clearance behind it are measured in R while the four joints and the two member lengths are not — so changing R moves the head's **travel** out from under a head that stayed where it was. Reproduced from the maintainer's *"when I re-size objects, cylinders become weird visually — try a fully expanded cylinder, then reduce the object size"*: a cylinder from (−3,0) to (3,0) at *Starts at* 100 %, Object Size halved, barrel and rod still 736.44 and the seal still 817.08 from the mount, but the head's travel now ends at 776.76 — **a gap of 40.32 between the barrel's mouth and the head**, the rod drawn from the head's own back edge, and the part in two pieces with daylight down the middle. Every *edit* of a cylinder already begins by putting the head back inside its travel; a change of size is not an edit, so nothing repaired it until the reader next touched the part — and the size changes by three routes (the Settings field, the "size for this zoom" button, and `SvgGridService.adoptScaleForDrawing` on load), all of which end in `MechanismService.applyObjectScaleChange`, which recomputed link outlines and nothing else. **What gives is the barrel, and only the barrel.** With mounts A and B and the seal S all held, N is the one joint left — derived, hidden, in no count a reader sees (S11, S20) — so the repair is one number, and it is the same `barrelHolding` a typed Rod Length already climbs to (one function, two doors; the sweep and its refusals are `model/cylinder-interiors.ts`, which also took over the per-rebuild derivation so the two cannot grow separate ideas of what a cylinder may move). The rod cannot help: its length is the distance between two joints that are both staying. A barrel **holding its length** (S5) does not give, and then the part is left exactly as it is and said so; so is one whose slide stands closer to its end joint than the shortest legal barrel will fit between, where no barrel reaches and only moving a joint the reader can see would. **Both notices are worded in the reader's own nouns** — *joint B*, *joint A*, never a mount, a head or a seal (`docs/ui-vocabulary.md`: the black block is a joint and is called one), and a spec forbids those four words in either sentence. Both name Object Size as the way out, as readiness's `cylinder-has-no-travel` already does. **Where the reach and the rod's floor disagree, the reach wins** — the one thing S29 gives up. `rod + clearance` is the longest barrel whose travel the rod is long enough for, and an edit that wants a longer one grows the rod, which moves mount B. This may not. A part drawn in two pieces is what the reader is looking at; the rod's floor is a rule about a pose the part is not in, and it carries a whole head of margin over the geometry it protects (B leaves the mouth at full retraction while `rod ≥ barrel − clearance` still has `head` in hand). A cylinder drawn with equal members clears that floor by exactly one clearance and loses `headAlongHalfMax` of head per unit of R, so the two conflict below **r′/r = 3.84/5.24 ≈ 0.733** — which is most of the size reductions anybody tries, including the maintainer's halving. **Undo, the URL and opening somebody's link.** Object Size rides the URL, so a repair is real geometry and has to be reachable. A size the *reader* chose earns **one** entry covering both: `applyObjectScaleChange` saves when it repaired anything, and remembers across the two passes a scale change makes while the Settings panel is open (the panel's own subscription, then whatever set the value) because the second pass finds a drawing already put right. A size the **app** adopted on load writes none: a drawing that was whole when it was saved is whole when it is opened at its saved size, so the question only arises because the app is about to change that size — and `adoptScaleForDrawing` already revises the state the drawing *arrived in* rather than adding to it, for exactly this reason about mark size, so the repair runs inside that revision and rides it. `SettingsService.objectScaleAdopting` is what holds the save for the duration, beside `objectScaleChosen` and for the same kind of reason: the act cannot be inferred from the value. **And *Starts at* is a reading, not a possession.** With every visible joint fixed and the lengths fixed, `start = (a − (c + hₘₐₓ)·r) / (L − c·r)`, where `a` is the seal's distance from the mount and `L` the barrel: both terms move with R and neither cancels, so `start′ = start` only at `r′ = r`. A reader's 100 % therefore **cannot** survive in general — and it does survive *downward*, because at 100 % the head is at the mouth, shrinking R takes the mouth off it, and the repair puts the mouth back: `L′ = a − hₘₐₓ·r′` gives `start′ = 1` exactly. Upward it does not: the head simply grows further back into a barrel that did not move, the part stays whole, nothing is repaired, and 100 % reads as 88.1 % at twice the size on the drawing above. That is the honest number and the panel shows it; what survives a size change is the **pose** — every joint, and both lengths unless the barrel had to give — and never a percentage. The panel cannot show one out of range because `cylinderSizeAt` clamps to 0–1 and reports 0 where there is no usable travel. Everything measured in R is refreshed by the same call: the skin and the fused silhouettes (`refreshSkinSilhouettes`), the slider plates, the slot channels, the ghost (`ghostCuts` is keyed on the scale), the report drawing (which clones the canvas), and the solved cycle itself — the stroke is the barrel's, so a size change re-solves it, and `cylinderRevision` is bumped by the rebuild so the stroke warning is asked again. `e2e/cylinder-object-size.mjs` drives the real Settings field, the real button and a drawing that adopts a size, and measures the gap between the mouth and the head off the DOM paths. *(September 21, 2026.)* |

> **Met.** Stage 2 was built in six packages — 2a, 2b, and 2c in four parts (canvas, menu, panel,
> mark) — with the removal below as the seventh. No solved number, template payload or fixture URL
> changed anywhere in it, and every URL written before it re-encodes byte for byte. The only thing
> that moved in a baseline is a *name*: the three shipped templates whose seal was stored under an
> interior name have it re-lettered on decode (S9), so `template-baseline.ts` carries one different
> sample id in each. The paragraph under each package says what it built and what it left behind.
>
> **The review of the pull request added three decisions, and one of them opened the format.**
> S14 (a member's inertia is derived, so a member offers Mass alone), S15 (the rod's own color)
> and S16 (a welded end is one body). S15 is the one that writes something new: a `KR<link>` entry
> in the URL's trailing section, beside the other part colors and only for a rod somebody gave a
> color of its own — so an older build refuses a drawing that uses it, the way it refuses any part
> color it does not know, and reads every other drawing exactly as before.

**2a. Record and derivation (files: `model/cylinder.ts`, `model/slide-assembly.ts`,
`model/cylinder-pose-plan.ts`, `services/transcoding/*`).** Replace role inference
(`resolveCylinder`, `sealedCylinderAt`, collinearity tolerance) with a lookup from the seal;
replace pose repair (`normalizeSealedCylinders`, `applyCylinderPose`) with derivation of N from
A, the barrel length and the angle. The reader keeps accepting old sealed five-point payloads and
folds them (after 1a's fold there are four joints; N is read and re-derived). Keep
`cylinderMembers`, the skin geometry and the stops as pure geometry.

> **Met.** `cylinderAtSeal` is the one lookup and reads the roles off the slot's own order;
> `derivedInterior` replaced the repair pass, and for a cylinder that is already straight it writes
> nothing. The record's fields are the plan's, and `isCylinderInterior` became the two predicates
> S11 asks for, every caller keeping the N-or-S one until the square became selectable in 2c.

**2b. Editing semantics (files: `services/grid-utils.service.ts`, `services/mechanism.service.ts`,
`model/hold-solver.ts`, `edit-panel`).** One angle (D10): editing Barrel Angle, Rod Angle or the
slide's Slider Angle rotates A–N–S–B about the slide joint S, or about a grounded mount when one
is grounded (if both are, the angle is fixed and the edit is refused). Starts at (D11) with its
tiebreak order and refusal.
Lengths: Barrel Length moves N (and the stroke); Rod Length moves B, unless B is grounded, in
which case A moves, with the same held-length and refusal rules as D11. A dragged mount keeps
today's behavior (extension changes; the skin stretches at the stops). All of this is one
transaction with one undo entry and the refusal wording from `docs/ui-vocabulary.md`.

> **Met, with one addition the decisions forced.** `model/cylinder-edit.ts` answers each typed or
> dragged edit with a pose or a refusal, and `runEdit` makes every one of them a single undo entry
> that changes nothing when refused. "Equal by construction" went with it (S3): the travel is the
> barrel's alone and the span is the seal's place along it plus the rod, so a new cylinder is still
> drawn with the two equal and every number an existing drawing produces is the number it produced
> before. The addition is the four refusal codes the ladder needs — `cylinder.angle-refused`,
> `cylinder.start-refused`, `cylinder.barrel-length-refused`, `cylinder.rod-length-refused`.

**2c. UI (files: `component/new-grid/*`, `edit-panel`, `context-menu-builder`,
`services/slider-mark.service.ts`).** The square is selectable and is joint S (D9); Barrel and
Rod panels (D12); mounts (D13); counts (D14); the cylinder's menus as drawn ("inside a cylinder",
"ground an end joint instead", "Delete Joint (and Cylinder)"). Selecting a member selects the member,
not the cylinder; the existing "Edit Cylinder" panel is retired. Gate: `e2e/cylinder-mount.mjs`,
`cylinder-mount-render`, `cylinder-drag`, `cylinder-panel`, `cylinder-skin`, `cylinder-end-on-joint`,
`context-menu`, `joint-type`, plus a new `e2e/cylinder-members.mjs` for D10, D11 (every tiebreak
branch and the refusal), D12 and D13, with filmstrips of a full out-and-back cycle.

> **Met, in four packages rather than one, and with one decision taken along the way.** The
> *canvas* made the black block joint S's own marker, hitbox and drag, gave the barrel's path and
> the rod's path a selection each, and lettered a new cylinder's two end joints before its seal —
> an old payload's interior-named seal being re-lettered as the last step of the build (S9). The
> *menu* gave the slide, the two end joints and the two members a card each and retired the
> whole-cylinder card, every refusal quoted from the model that enforces it and every count a
> reader sees going through `MechanismService.visibleJoints`. The *panel* retired Edit Cylinder for
> a Barrel panel, a Rod panel and the slide's own, with Travel becoming the barrel's Length and the
> three masses each their own part's. The fourth is **decision S13**, which the maintainer asked for
> once the square had become a joint: a slider whose riders cannot
> turn is drawn as a cream rounded bar lying along its slot, the bar is the joint and the black
> block under it is furniture.
>
> **Two exceptions are recorded rather than fixed.** The library thumbnails (`src/assets/gifs`)
> and the README shots (`docs/images/readme`) still show the old `+` and bare-head marks; retaking
> them rewrites tracked binaries and is the maintainer's call. The DXF keeps its cross at a slide
> deliberately (`semantic-dxf.ts`): line art has no fill for a bar to be.

**2d. Removal.** Delete what no caller needs: role inference, pose repair, the five-point
construction in creation (creation builds A, N, S, B directly), hidden interior hitboxes, the
"is ram / carries ram" queries. Rewrite `cylinder-weld-guards.spec.ts` to the new boundary. Update
`CLAUDE.md` and the reference documents the change reaches; note in
`docs/cylinder-mount-joints-plan.md` that its "future direction" row is now built.

> **Met.** Role inference and pose repair had already gone in 2a and the Edit Cylinder panel's own
> writers in 2c, so what was left was the vocabulary around them: the model-level membership
> helpers no caller had left (`cylinderOfJoint`, `cylindersOfJointIn`, `cylinderMounts`,
> `isCylinderMount`, `cylinderMountsAt`), `MechanismService.cylinderOfLink` — a third name for the
> identity question — and `toggleCylinderInput`, which the panel and the menu had both stopped
> using once the drive became the slide's own. `cylinder-pose-plan.ts`'s eight exports with no
> outside caller are file-private. `cylinderAt` (carrying) and `cylinderOfBar` (identity) both stay:
> a welded end joint is exactly what makes the two differ.
>
> Three things went further than the list. **The letter rule was written twice** — once in
> `MechanismService.determineNextLetter` and once, deliberately, in the URL builder — and is now
> `model/joint-letters.ts`, asked by both and unit-tested on its own. **`MARK.arrowTail` and
> `MARK.slideAlongHalf` were both 1.4 by coincidence**, which is the only reason a driven slide's
> arrows meet its mark; `joint-marks.spec.ts` pins the pair. And the Edit panel kept **a second
> door to the drive**: a `jointForm` control named `input`, bound to nothing since Add Input became
> a button on `adjustInput`, writing the flag straight onto the joint and rebuilding *without* an
> undo entry. `e2e/posed-edit-audit.mjs` is what found it, on the slide of `Cylinder_Boom`;
> `input-toggle.spec.ts` now pins both directions of the one door that is left.
>
> `cylinder-weld-guards.spec.ts` is rewritten to the boundary S11 draws: what is refused is
> welding, unwelding, sliding, merging onto and slotting **N or S**, what is allowed is everything
> on an end joint, and each assertion reads the refusal off the model rather than spelling it out.
> Documents updated: `CLAUDE.md`, `docs/README.md`, `docs/short-notes.md`, `docs/domain-facts.md`,
> `docs/ui-vocabulary.md`, `docs/ui-copy-audit.md` and `docs/cylinder-mount-joints-plan.md`. The
> instruction here used to name `docs/tips-and-tricks.md`, which was split into
> `environment.md`, `ui-gotchas.md`, `domain-facts.md` and `short-notes.md` long before this stage;
> `notification-inventory.md` is deliberately untouched, because it is the record of the snackbar
> as it was and not an inventory of what the app says now.

## Order and what each stage must not do

R → 0a → 0b and 0c in parallel → 1a → 1b and 1c in parallel → 1d → 2a → 2b → 2c → 2d. Stage 0
must not touch the codec. Stage 1 must not change any solved number. Stage 2 must not keep a
second cylinder representation alive beside the seal. Split Joint comes after 2d, as its own plan
section when it is scoped.
